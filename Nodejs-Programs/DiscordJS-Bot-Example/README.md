# discord.js Bot Example

**To install and run the bot**

```bash
    npm install (Installs All Necessary Node Modules)
    node . (Run the Bot)
```

**Check config.json for all the variables needed for the bot**

**Greetings!**
